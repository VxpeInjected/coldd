.claude/settings.local.json
supabase/.temp/

# Ephemeral impeccable session state - not site content, and it was
# being published on the live domain.
.impeccable/questions/
test.txt

# local scratch / preview files
scratchpad/
.DS_Store
